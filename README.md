# crud-articles
crud articles rtk, rtk-query, react,ts

## Development

The site uses some templating via 11ty. 

```bash
# install dependencies
npm i

# build
npm run build

# start
npm run start
```
